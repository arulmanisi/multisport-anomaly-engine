You are my coding agent.

Goal:
Update README.md to document how to run the backend and the Streamlit UI for the Plaix.ai demo, including the PLAIX_API_URL environment variable.

Tasks:

1) Add or update a section "Running the Backend"
- Explain briefly how to start the FastAPI backend from the repo root, for example:

  ```bash
  cd backend
  uvicorn app.main:app --reload --port 8000
